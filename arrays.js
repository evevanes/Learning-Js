var student = ["Jane", "Lauren", "Ivy", "Mary", "Fatma", "Irene"]
var shoeSize = [4, 5, 6, 7, 8]

// Get the length of the array
console.log(student.length)

for(i=0;i<student.length;i++){
	console.log(student[i]);
}

